file(GLOB DXTEX_SOURCES "*.cpp") 

# 라이브러리 타겟 정의 (정적 라이브러리)
add_library(DirectXTex_Lib STATIC ${DXTEX_SOURCES})

# 헤더 경로 설정 (메인 프로젝트가 찾을 수 있도록 PUBLIC으로 설정)
# 이 라이브러리가 설치된 폴더를 include 경로로 추가합니다.
target_include_directories(DirectXTex_Lib PUBLIC
    .
)